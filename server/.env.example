# Copy to /etc/gutlog-api.env (root-only, chmod 600) and fill in.
API_TOKEN=            # npm run token
ANTHROPIC_API_KEY=    # for /analyze only
ALLOWED_ORIGINS=https://jgr3go.github.io
DB_PATH=/var/lib/gutlog-api/gutlog.db
PORT=8787
